import React from "react";

/**
 * Imports other components and hooks.
 */
import Home from ".";

/**
 * Displays the Home demo.
 */
const HomeDemo = (props) => {
  return <Home />;
};

export default HomeDemo;
